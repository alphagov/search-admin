<%= render "govuk_publishing_components/components/breadcrumbs", {
  breadcrumbs: [
    {
      title: "Queries",
      url: queries_path
    },
    {
      title: "New query"
    }
  ]
} %>

<%= render "common/page_title", title: "New query" %>

<%= render 'form', query: Query.new %>
