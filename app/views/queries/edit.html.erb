<%= render "govuk_publishing_components/components/breadcrumbs", {
  breadcrumbs: [
    {
      title: "Queries",
      url: queries_path
    },
    {
      title: @query.display_name,
      url: query_path(@query)
    },
    {
      title: "Edit"
    }
  ]
} %>

<%= render "common/page_title", title: @query.display_name %>

<%= render 'form', query: @query %>
