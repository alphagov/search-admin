<%= render "govuk_publishing_components/components/breadcrumbs", {
  breadcrumbs: [
    {
      title: "Queries",
      url: queries_path
    },
    {
      title: "Bets for '#{@bet.query.display_name}'",
      url: query_path(@bet.query)
    },
    {
      title: params["active"] == "true" ? "Activate '#{@bet.link}'" : "Edit '#{@bet.link}'"
    }
  ]
} %>

<%= render "common/page_title", title: params["active"] == "true" ? "Activate '#{@bet.link}'" : "Edit '#{@bet.link}'" %>

<%= render 'form', bet: @bet, query: @bet.query %>
