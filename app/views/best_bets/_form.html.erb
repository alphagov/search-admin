<%= form_for(best_bet) do |f| %>
  <%= f.text_field :query %>
  <%= f.select :match_type, [['Exact', 'exact'], ['Stemmed', 'stemmed']] %>
  <%= f.text_field :link %>
  <%= f.number_field :position %>
  <%= f.text_field :comment %>

  <%= f.button "Save", class: 'btn btn-primary' %>
<% end %>
